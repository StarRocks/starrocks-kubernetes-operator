---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: starrocksclusters.starrocks.com
spec:
  group: starrocks.com
  names:
    kind: StarRocksCluster
    listKind: StarRocksClusterList
    plural: starrocksclusters
    singular: starrockscluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StarRocksCluster is the Schema for the starrocksclusters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: StarRocksClusterSpec defines the desired state of StarRocksCluster
            properties:
              StarRocksCnSpec:
                description: StarRocksCnSpec is the cn specification.
                properties:
                  image:
                    description: Image is the container image for the cn.
                    type: string
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources
                      allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  name:
                    description: name of the starrocks be cluster.
                    pattern: '[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*'
                    type: string
                  probe:
                    description: Probe defines the mode probe service in container
                      is alive.
                    properties:
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before liveness probes are initiated. Default to 10 seconds.
                        format: int32
                        minimum: 0
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                          Default to Kubernetes default (10 seconds). Minimum value
                          is 1.
                        format: int32
                        minimum: 1
                        type: integer
                      type:
                        description: Type identifies the mode of probe main container
                        enum:
                        - tcp
                        - command
                        type: string
                    required:
                    - type
                    type: object
                  replicas:
                    description: Replicas is the number of desired cn Pod.
                    format: int32
                    minimum: 0
                    type: integer
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute
                      resources required. If Requests is omitted for a container,
                      it defaults to Limits if that is explicitly specified, otherwise
                      to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  service:
                    description: Service defines the template for the associated Kubernetes
                      Service object. the service for user access cn.
                    properties:
                      name:
                        type: string
                      ports:
                        items:
                          properties:
                            containerPort:
                              format: int32
                              type: integer
                            name:
                              type: string
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              format: int32
                              type: integer
                          required:
                          - containerPort
                          - port
                          type: object
                        type: array
                    required:
                    - ports
                    type: object
                required:
                - image
                type: object
              serviceAccount:
                description: Specify a Service Account for starRocksCluster
                type: string
              starRocksBeSpec:
                description: StarRocksBeSpec is the be specification.
                properties:
                  ReplicaInstances:
                    description: ReplicaInstance is the names of replica starrocksbe
                      cluster.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the container image for the be.
                    type: string
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources
                      allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  name:
                    description: name of the starrocks be cluster.
                    pattern: '[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*'
                    type: string
                  probe:
                    description: Probe defines the mode probe service in container
                      is alive.
                    properties:
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before liveness probes are initiated. Default to 10 seconds.
                        format: int32
                        minimum: 0
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                          Default to Kubernetes default (10 seconds). Minimum value
                          is 1.
                        format: int32
                        minimum: 1
                        type: integer
                      type:
                        description: Type identifies the mode of probe main container
                        enum:
                        - tcp
                        - command
                        type: string
                    required:
                    - type
                    type: object
                  replicas:
                    description: 'Replicas is the number of desired be Pod Optional:
                      Defaults to 3'
                    format: int32
                    type: integer
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute
                      resources required. If Requests is omitted for a container,
                      it defaults to Limits if that is explicitly specified, otherwise
                      to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  service:
                    description: Service defines the template for the associated Kubernetes
                      Service object. the service for user access be.
                    properties:
                      name:
                        type: string
                      ports:
                        items:
                          properties:
                            containerPort:
                              format: int32
                              type: integer
                            name:
                              type: string
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              format: int32
                              type: integer
                          required:
                          - containerPort
                          - port
                          type: object
                        type: array
                    required:
                    - ports
                    type: object
                  storageVolumes:
                    description: StorageVolumes defines the additional storage for
                      fe.
                    items:
                      description: StorageVolume defines additional PVC template for
                        StatefulSets and volumeMount for pods that mount this PVC
                      properties:
                        mountPath:
                          description: specify the path of volume mount.
                          type: string
                        name:
                          description: name of a storage volume.
                          type: string
                        storageClassName:
                          description: 'storageClassName is the name of the StorageClass
                            required by the claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1'
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                required:
                - image
                type: object
              starRocksFeSpec:
                description: StarRocksFeSpec is the fe specification.
                properties:
                  ReplicaInstances:
                    description: ReplicaInstance is the names of replica starrocksbe
                      cluster.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the container image for the be.
                    type: string
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources
                      allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  name:
                    description: name of the starrocks be cluster.
                    pattern: '[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*'
                    type: string
                  probe:
                    description: Probe defines the mode probe service in container
                      is alive.
                    properties:
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before liveness probes are initiated. Default to 10 seconds.
                        format: int32
                        minimum: 0
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                          Default to Kubernetes default (10 seconds). Minimum value
                          is 1.
                        format: int32
                        minimum: 1
                        type: integer
                      type:
                        description: Type identifies the mode of probe main container
                        enum:
                        - tcp
                        - command
                        type: string
                    required:
                    - type
                    type: object
                  replicas:
                    description: 'Replicas is the number of desired be Pod Optional:
                      Defaults to 3'
                    format: int32
                    type: integer
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute
                      resources required. If Requests is omitted for a container,
                      it defaults to Limits if that is explicitly specified, otherwise
                      to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  service:
                    description: Service defines the template for the associated Kubernetes
                      Service object. the service for user access be.
                    properties:
                      name:
                        type: string
                      ports:
                        items:
                          properties:
                            containerPort:
                              format: int32
                              type: integer
                            name:
                              type: string
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              format: int32
                              type: integer
                          required:
                          - containerPort
                          - port
                          type: object
                        type: array
                    required:
                    - ports
                    type: object
                  storageVolumes:
                    description: StorageVolumes defines the additional storage for
                      fe.
                    items:
                      description: StorageVolume defines additional PVC template for
                        StatefulSets and volumeMount for pods that mount this PVC
                      properties:
                        mountPath:
                          description: specify the path of volume mount.
                          type: string
                        name:
                          description: name of a storage volume.
                          type: string
                        storageClassName:
                          description: 'storageClassName is the name of the StorageClass
                            required by the claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1'
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                required:
                - image
                type: object
            type: object
          status:
            description: StarRocksClusterStatus defines the observed state of StarRocksCluster
            properties:
              phase:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              starRocksBeStatus:
                properties:
                  creatingInstances:
                    items:
                      type: string
                    type: array
                  failedInstances:
                    items:
                      type: string
                    type: array
                  phase:
                    type: string
                  reason:
                    type: string
                  resourceNames:
                    items:
                      type: string
                    type: array
                  runningInstances:
                    items:
                      type: string
                    type: array
                  serviceName:
                    type: string
                required:
                - phase
                - reason
                type: object
              starRocksCnStatus:
                properties:
                  creatingInstances:
                    items:
                      type: string
                    type: array
                  failedInstanceNames:
                    items:
                      type: string
                    type: array
                  phase:
                    type: string
                  reason:
                    type: string
                  resourceNames:
                    items:
                      type: string
                    type: array
                  runningInstances:
                    items:
                      type: string
                    type: array
                  serviceName:
                    type: string
                required:
                - phase
                - reason
                type: object
              starRocksFeStatus:
                description: used pointer for the component
                properties:
                  creatingInstances:
                    items:
                      type: string
                    type: array
                  failedInstances:
                    items:
                      type: string
                    type: array
                  phase:
                    type: string
                  reason:
                    type: string
                  resourceNames:
                    items:
                      type: string
                    type: array
                  runningInstances:
                    items:
                      type: string
                    type: array
                  serviceName:
                    type: string
                required:
                - phase
                - reason
                type: object
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
